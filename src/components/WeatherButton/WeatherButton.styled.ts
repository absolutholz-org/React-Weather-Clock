import styled from 'styled-components';

export const Button = styled.button`
    > svg {
        display: flex;
        font-size: min(12vmin, 4rem);
        height: 1em;
        width: auto;
    }
`;
